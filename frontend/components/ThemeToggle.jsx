"use client";import{Moon,Sun}from"lucide-react";import{useTheme}from"next-themes";import{useEffect,useState}from"react";export function ThemeToggle(){const{resolvedTheme,setTheme}=useTheme(),[ready,setReady]=useState(false);useEffect(()=>setReady(true),[]);return <button aria-label="Đổi giao diện sáng tối" className="btn-secondary h-10 w-10 p-0" onClick={()=>setTheme(resolvedTheme==="dark"?"light":"dark")}>{ready&&resolvedTheme==="dark"?<Sun size={18}/>:<Moon size={18}/>}</button>}
