"use client";import{useEffect}from"react";import{useRouter}from"next/navigation";import{useAuth}from"@/contexts/AuthContext";import{Loading}from"./States";export default function Protected({children,admin=false}){const{user,loading,isAdmin}=useAuth(),router=useRouter();useEffect(()=>{if(!loading&&(!user||(admin&&!isAdmin)))router.replace(user?"/":"/dang-nhap")},[user,loading,isAdmin,admin,router]);if(loading||!user||(admin&&!isAdmin))return <Loading label="Đang kiểm tra quyền truy cập"/>;return children}
